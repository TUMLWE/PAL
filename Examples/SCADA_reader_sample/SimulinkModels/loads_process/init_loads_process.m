function init_loads_process(params_filename)%this is the initialization file for the loads_process Simulink model, here you can specify constants and variables necessary for running and compiling your model 

load(params_filename)
% Variable definition






% Sect 2: assignments. Here all the defined variables that are useful for running the model are assigned to the parameter file
% save(params_filename,var_name,'-append')  %prototype of the saving call

end 
